{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/build/bin/rocket_game.exe",
            "args": [],
            "cwd": "${workspaceFolder}/build/bin",
            "preLaunchTask": "CMake: build"
        }
    ]
}